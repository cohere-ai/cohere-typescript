/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Cohere from "../index.js";

/**
 * A streamed event which signifies that the chat message has ended.
 */
export interface ChatMessageEndEvent extends Cohere.ChatStreamEventType {
    id?: string;
    delta?: ChatMessageEndEvent.Delta;
}

export namespace ChatMessageEndEvent {
    export interface Delta {
        /** An error message if an error occurred during the generation. */
        error?: string;
    }
}
